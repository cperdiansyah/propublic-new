import { OAuthCallbackPage } from '@/features/auth'

/**
 * OAuth Redirect Callback Route
 * Handles OAuth redirect callback outside the main app layout
 */
export default function RedirectCallbackRoute() {
  return <OAuthCallbackPage />
}
